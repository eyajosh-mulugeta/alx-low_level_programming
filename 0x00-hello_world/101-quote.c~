#include <stdio.h>

/**
 * main - the main function prints a string message to the console.
 * Return: Always 0 (Success)
 */
int main(void)
{
fprintf("and that piece of art is useful\"\
-Dora Korpar, 2015-10-19\n", stderr);
return (0);
}
